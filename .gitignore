# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
.python-version.local

# Tooling caches
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# Environment / secrets — never commit credentials
.env

# Data — raw/processed data stays out of git (HF dataset is CC BY 4.0, re-downloadable)
# Root-anchored so it does not swallow src/dbahn_delay/data
/data/

# Experiment tracking artifacts
mlruns/
mlartifacts/

# Exported model bundles — versioned via MLflow, not git
/models/

# Notebooks
.ipynb_checkpoints/

# Personal study notes — local only
/notes/

# OS / editor
.DS_Store
.idea/
.vscode/

# Temp training artifacts (uploaded to MLflow)
/mlruns_artifacts_tmp/
